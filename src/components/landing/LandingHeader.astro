---
interface Props {
  appUrl: string;
}

const { appUrl } = Astro.props;
---

<header class="sticky top-0 z-50 border-b border-neutral-200/60 bg-white/80 backdrop-blur">
  <div class="mx-auto flex max-w-6xl items-center justify-between px-4 py-4">
    <div class="flex items-center gap-2">
      <div class="h-8 w-8 rounded-xl bg-neutral-900"></div>
      <span class="text-sm font-semibold tracking-tight">Nexuit</span>
    </div>

    <nav class="hidden items-center gap-6 text-sm text-neutral-600 md:flex">
      <a class="hover:text-neutral-900" href="#producto">Producto</a>
      <a class="hover:text-neutral-900" href="#modulos">Módulos</a>
      <a class="hover:text-neutral-900" href="#para-quien">Para quién</a>
      <a class="hover:text-neutral-900" href="#faq">FAQ</a>
    </nav>

    <div class="flex items-center gap-2">
      <a
        href={appUrl}
        class="rounded-xl bg-neutral-900 px-4 py-2 text-sm font-medium text-white hover:bg-neutral-800"
        rel="noopener noreferrer"
      >
        Acceder
      </a>
    </div>
  </div>
</header>
