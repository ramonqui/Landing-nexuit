---
interface FaqItem {
  q: string;
  a: string;
}

interface Props {
  appUrl: string;
  primaryCtaClass: string;
  secondaryCtaClass: string;
  faqs: FaqItem[];
}

const { appUrl, primaryCtaClass, secondaryCtaClass, faqs } = Astro.props;
---

<section id="faq" class="mx-auto max-w-6xl px-4 py-14">
  <h2 class="text-2xl font-semibold tracking-tight md:text-3xl">FAQ</h2>

  <div class="mt-8 grid gap-4 md:grid-cols-2">
    {faqs.map((faq) => (
      <details class="rounded-2xl border border-neutral-200 p-6">
        <summary class="cursor-pointer text-sm font-semibold">{faq.q}</summary>
        <p class="mt-2 text-sm text-neutral-600">{faq.a}</p>
      </details>
    ))}
  </div>

  <div class="mt-10 rounded-2xl border border-neutral-200 bg-neutral-50 p-8">
    <h3 class="text-lg font-semibold">¿Listo para ordenar tu operación base?</h3>
    <p class="mt-2 max-w-2xl text-sm leading-6 text-neutral-600">
      Entra a Nexuit y arranca con Inventario + Productos. Si hoy operas con hojas sueltas o
      sistemas desconectados, este es el punto donde empiezas a trabajar con una sola fuente de
      verdad.
    </p>
    <div class="mt-5 flex flex-col gap-3 sm:flex-row">
      <a href={appUrl} class={primaryCtaClass} rel="noopener noreferrer">
        Acceder a Nexuit
      </a>
      <a href="#producto" class={secondaryCtaClass}>
        Ver el problema que resuelve
      </a>
    </div>
  </div>
</section>
